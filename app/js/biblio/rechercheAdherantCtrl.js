angular.module("biblio")
   .controller('rechercheAdherantCtrl', ['$scope', '$filter', function ($scope, $filter) {
        $scope.items = [
            /*{ id: 1, name: 'ABC' },
            { id: 2, name: 'DEF' },
            { id: 3, name: 'GHI' },
            { id: 4, name: 'Jkl' },
            { id: 5, name: 'ABCD' }];
        $scope.items2 = $scope.items;
        $scope.$watch('search', function (val) {
            $scope.items = $filter('filter')($scope.items2, val);*/

    ]}]);